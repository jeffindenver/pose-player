// on user action, show directory picker
// use window.showDirectoryPicker
// the directory picker returns a handle

// FileSystemDirectoryHandle.entries() returns an array of
// the objects properties

function DirectoryReader(directoryHandle) {
  "use strict";

  console.log("Inside directoryReader");
  console.log(directoryHandle);


}
